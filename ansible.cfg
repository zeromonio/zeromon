# https://zeromon.io/
# https://github.com/zeromonio/zeromon
[defaults]
remote_tmp=/tmp/ansible_remote
local_tmp=/tmp/ansible_local
retry_files_enabled=False
ansible_managed=ANSIBLE MANAGED / see https://github.com/zeromonio/zeromon for more information
